# Database Test Suite

This folder contains example pytest modules for database validation scenarios such as connectivity, schema checks, constraints, data quality, performance, partitions, indexes, and statistics.

## Suggested Usage

- Run the suite with `pytest automation/pytest/database`
- Extend the placeholder tests with real database connections and assertions
- Keep SQL validation queries in the companion sample SQL file
