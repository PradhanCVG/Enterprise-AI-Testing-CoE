[pytest]

python_files = test_*.py

python_classes = Test*

python_functions = test_*

addopts =
    -v
    --tb=short

markers =
    database
    oracle
    postgresql
    etl
    data_quality
    chunking
    embeddings
    vector
    rag
    guardrails
    performance